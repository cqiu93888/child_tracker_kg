# 僅部署 api_cloud（Fly.io / 其他容器平台）
FROM python:3.11-slim
WORKDIR /app
COPY requirements-cloud.txt .
RUN pip install --no-cache-dir -r requirements-cloud.txt
COPY api_cloud.py .
EXPOSE 8080
ENV PORT=8080
CMD ["sh", "-c", "exec uvicorn api_cloud:app --host 0.0.0.0 --port ${PORT}"]
